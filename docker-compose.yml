version: '3.8'

services:
  thesis-web:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - "1051:1051"
    environment:
      - NODE_ENV=production
      - PORT=1051
      - HOSTNAME=0.0.0.0
    restart: unless-stopped

